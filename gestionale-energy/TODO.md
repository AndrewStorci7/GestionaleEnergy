# Task list 16/02/26

> :warning: Attenzione! \
> Ogni volta che esegui una task e la finisci, in questo file vai a riempire la riga in questo modo: \
> ```
> - [x]: testo bla bla bla ...
> ```
> e sotto crei un titolo in questo modo: `##### Lavoro svolto` e sotto una breve descrizione di quello che è stato fatto 

## Creare un nuovo branch

Nel caso non l'avessi fatto, crea una nuovo branch dove fare le seguenti modifiche, sempre con il prefisso `dev/`

## Descrizione

Migliroare la disposizione delle componenti per quanto riguarda i dispositivi mobile. Cambiare la posizione degli elementi e dell'altezza.

## Lista

### Parte 1

#### Obbligatorie

- [ ]: Rendere la componente **"Filtra"** nascondibile con un bottone, che può essere tranquillamente definito all'interno della gestione del **filtra**.

#### Facoltiva

- [ ]: Si può pensare di ridurre il font generico della pagina in caso di mobile; parlarne con Gianluca oppure valuta tu stesso.

### Parte 2

- [ ]: Modificare la disposizione delle componenti: `BtnWheelman`, `BtnPresser` e `Footer`. Risultano mal posizionate in caso di ridimensione della pagina (sia in altezza che in larghezza); quasi sicuramente avranno un `position absolute` che li va a posizionare, perciò bisogna cambiarlo per fare in modo che sia dinamico e che si posizionino smepre sotto la tabella (**altrimenti valutare se spostare i bottoni SOPRA la tabella, valutare con Gianluca**).

